# Streamlit Configuration

[theme]
primaryColor = "#1f77b4"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f0f2f6"
textColor = "#262730"
font = "sans serif"

[server]
headless = false
port = 8501

# Note: hideSidebar, hideTopBar, and hideFooter are not valid config options
# Use st.set_page_config(initial_sidebar_state="expanded") in code instead

[client]
showErrorDetails = true

